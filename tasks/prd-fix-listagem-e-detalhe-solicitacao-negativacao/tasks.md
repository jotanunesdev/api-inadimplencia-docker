# Resumo de Tarefas - Fix de Listagem e Detalhe de Solicitacao de Negativacao

## Tarefas

- [x] 1.0 Adicionar `ListByStatusAsync` em `ISerasaPefinRepository` + implementacao SQL (Complexidade: MEDIUM)
- [x] 2.0 Expandir `ListSolicitacoesPendentesQuery` e implementar handler real (Complexidade: MEDIUM)
- [x] 3.0 Criar `GetSolicitacaoByIdQuery` + handler + DTO `SolicitacaoDetalheDto` (Complexidade: MEDIUM)
- [x] 4.0 Atualizar endpoints REST `GET /solicitacoes` e `GET /solicitacoes/{id}` (Complexidade: LOW)
- [x] 5.0 Testes unitarios e de integracao (Complexidade: MEDIUM)
